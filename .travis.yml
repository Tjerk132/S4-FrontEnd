language: node_js
node_js:
  - "stable"

cache: npm

script: 
  # - npm test
  - npm build
deploy:
  provider: pages
  cleanup: false
  github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  on:
    branch: master